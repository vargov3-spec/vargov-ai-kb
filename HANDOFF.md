# Бриф для агента, разрабатывающего сайт vargov.ru

Ты делал сайт и знаешь его устройство — эта вводная не про сайт, а про то, что произошло в параллельной облачной сессии Claude и что из этого нужно интегрировать.

Дата: 2026-08-09.

**Всё лежит в этом репозитории — `vargov3-spec/vargov-ai-kb`, ветка `claude/configurator-continuation-3n0vib`.** Внимание: это НЕ ветка по умолчанию; обычный clone даст `main` без этих материалов.

```bash
git clone -b claude/configurator-continuation-3n0vib https://github.com/vargov3-spec/vargov-ai-kb.git
```

**Если git clone / curl в твоей сессии блокируются** («Failed to run»: режим Auto при недоступном классификаторе безопасности), есть два обхода:

1. Владелец переключает режим разрешений с Auto на обычный (селектор слева внизу у поля ввода, или Shift+Tab) и подтверждает команды вручную — это сбой классификатора, а не запрет сети.
2. Совсем без git: владелец скачивает ветку ZIP-архивом в браузере и распаковывает рядом с сайтом:
   `https://github.com/vargov3-spec/vargov-ai-kb/archive/refs/heads/claude/configurator-continuation-3n0vib.zip`
   (~10 МБ; корневая папка внутри архива — `vargov-ai-kb-claude-configurator-continuation-3n0vib`). Дальше работай с локальными файлами.

Проверка, что данные те: в `integration/products.json` — 605 SKU × 8 языков; в `content/journal/` — 8 папок по 22 статьи. Если видишь 594 текста и 47 батчей — это устаревший `main`, возьми ветку выше.

---

## 1. Что появилось нового

### Тексты карточек товаров — `content/product-descriptions/`

605 артикулов × 8 языков (ru, en, de, fr, it, es, vi, ar) = 4840 текстов. Русский в корне папки, переводы в подпапках. Покрытие каталога полное, включая 11 артикулов с суффиксами (LC0166-1 и т. п.).

Это **новые художественные тексты**, написанные по утверждённому владельцем формату (`content/product-descriptions/PLAN.md`). Они предназначены на замену текущих описаний на карточках: нынешние тексты сайта нарушают утверждённые правила — например, живая карточка LC0138 называет материалы («полированная латунная петля», «стеклянное кольцо»), что в новом формате запрещено (только видимые свойства и оттенки; правило действует для карточек, но **не** для статей журнала).

### Статьи журнала — `content/journal/<язык>/<slug>.md`

22 статьи × 8 языков. Версии en, de, fr, it, es сняты с живого сайта (источник правды на момент снятия); **vi и ar — новые переводы, на сайте этих локалей журнала нет вовсе** (`/vi/journal` и `/ar/journal` сейчас 404).

### Машиночитаемые данные — `integration/`

| Файл | Что |
|---|---|
| `by-locale/products.<язык>.json` | Карточки по языкам, 0,9–1,4 МБ. **Для страниц использовать эти** |
| `products.json` | Всё в одном файле, 8,5 МБ — как источник правды, не для импорта в страницы |
| `journal.json` | Статьи: типизированные блоки `p`/`h`/`li`, `dateISO`, `categoryId`, `metaLabels`, `rtlLocales` |
| `build-bundles.py` | Пересборка из `content/`; падает с ошибкой при битом исходнике |
| `README.md` | Полное описание схемы — прочитай перед интеграцией |
| `VERIFICATION.md` | Как проверялось + остаточные вопросы |

Качество: обратная сборка markdown из JSON совпала с исходниками побайтово, 0 расхождений на 4840 карточках и 176 статьях. Нюансы схемы, ради которых её и делали: подписи блоков карточек (`labels`) меняются по роду изделия (в ru 4 формы, в ar 2 — захардкодить нельзя); тело статей отдаётся блоками, чтобы списки не пришли строками с дефисами; `dateISO`/`categoryId` — для сортировки, `<time>`, JSON-LD и рубрик.

---

## 2. Задачи по сайту

1. **Локали `vi` и `ar`**: маршруты `/vi/journal/*`, `/ar/journal/*`, переключатель языков, hreflang. Для `ar` — `dir="rtl"` и проверка вёрстки (зеркальные отступы, стрелки навигации, выравнивание).
2. **Карточки товаров**: подключить тексты всех 8 языков из `integration/by-locale/`. Проверить, какие локали каталога вообще существуют сейчас.
3. **Обновить русские версии трёх статей** — в репозитории они свежее сайта (редакционные правки владельца, см. раздел 3): `fabrika`, `prototipirovanie`, `vysokoe-zhyuri`. Европейские версии этих статей на сайте уже соответствуют новым формулировкам, трогать их не надо.
4. **Опечатка на живом сайте**, `/es/journal/fabrika`: «América **o** Oriente Medio» → «América **u** Oriente Medio» (RAE: o→u перед словом на o-). В репозитории и пакетах уже исправлено — на сайте уйдёт сама при обновлении статьи из пакетов; если журнал интегрируется позже, поправь на сайте точечно.

Рабочий процесс: код сайта сейчас не в GitHub — см. раздел 4. Если интегрируешь до выгрузки, всё равно бери данные из этого репозитория, а не копипастой из чата.

---

## 3. Редакционные решения владельца — соблюдать

Приняты в этой сессии, уже применены к текстам в репозитории:

- **География производства не называется.** Фабрика в Китае — собственная, поэтому формулировка «наше собственное производство» (а не «фабрика в Китае»). Происхождение бренда: «известен за пределами домашнего рынка» (а не «за пределами России»).
- **Исключение — `istoriya-elementa`**: там Китай назван намеренно во всех восьми языках, вся аргументация статьи на этом построена. Не менять.
- Фактические упоминания России остаются: выставка в Москве (`interlight-2025`, `interlight-vystavka`), монтаж в России и ОАЭ (`montazh-ot`), заголовок «Приключения итальянцев в России».
- **Карточки товаров**: материалы элементов не называть (хрусталь, стекло, фарфор, керамика) — только видимые свойства. Полные правила в `PLAN.md`. На статьи журнала запрет не распространяется.

Если какие-то из этих формулировок конфликтуют с твоими исходниками текстов — приоритет у версий из этого репозитория, они новее и согласованы с владельцем.

---

## 4. Просьба владельца: выгрузить код сайта в GitHub

Код сайта существует только на этой машине; на сервере — лишь сборка без исходников. Ни резервной копии, ни истории, ни возможности для облачной сессии открывать PR.

Просьба: приватный репозиторий `vargov3-spec/vargov-site`, обычный первый пуш. Из очевидного, но критичного: `.gitignore` до первого коммита (`node_modules/`, `.next/`, `out/`, `.env*` кроме `.env.example`, логи), и глазами проверить `git status` на предмет секретов перед пушем.

После выгрузки имеет смысл повторить схему деплоя конфигуратора: сборка в GitHub Actions → VPS сам забирает артефакт по HTTPS (прямой SSH с раннеров на Timeweb блокируется ТСПУ). Готовый образец — в репозитории `vargov3-spec/vargov-configurator` (приватный): `.github/workflows/deploy-ru-vps.yml` и `deploy/pull-deploy.sh`.

---

## 5. Открытые вопросы к владельцу (не блокируют)

- **Даты в `istoriya-elementa`**: элемент «создан весной 2022», но «впервые опубликован 23 февраля 2022» (публикация раньше создания); в теле «видеоотчёт 8 декабря 2023», в блоке ссылок — «3 декабря 2023». Уточнить верные даты, затем синхронизировать восемь языков.
- **Повтор абзаца** в русских карточках LC0321, LC0323–LC0326, LC0328, LC0329 (абзац продублирован внутри «день/вечер»-абзаца; en-перевод дедуплицировал, остальные воспроизвели). Рекомендация — вычистить в ru и пересобрать пакеты. Низкий приоритет.

Полный чек-лист: `knowledge/journal-i18n-audit.md`.

---

## 6. Справочно

- Облачная сессия установила: VPS Timeweb `72.56.38.211`, nginx + Next.js standalone в `/var/www/vargov/ru/`; процесс называется `next-server (vX.Y.Z)`, поэтому `grep node` его не видит; `x-nextjs-prerender` дублируется в заголовках (add_header в nginx поверх заголовка приложения).
- Ветку `claude/configurator-continuation-3n0vib` стоит влить в `main` этого репозитория, когда владелец скажет — сейчас `main` сильно отстаёт.
- После правок текстов в `content/` пересобирай пакеты: `python3 integration/build-bundles.py` (ключ кэша — `meta.checksum`).
